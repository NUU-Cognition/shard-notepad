# Notepad

Notepad shard

## Structure

```
Shards/(Dev) Notepad/
  shard.yaml              # Manifest
  init-ntpd.md    # Init file — shard context
  skills/                 # sk-ntpd-{name}.md
  workflows/              # wkfl-ntpd-{name}.md
  templates/              # tmp-ntpd-{name}.md
  knowledge/              # knw-ntpd-{name}.md
  assets/                 # ast-ntpd-{name}.{ext}
  install/                # Files to install into Mesh/
```
